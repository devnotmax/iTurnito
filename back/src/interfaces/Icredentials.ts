interface Icredentials {
    id: number //ID numerico que identifica al par de credenciales.
    username: string    // Username de usuario que posee las credenciales.
    password: string // password del usuario que posee las credenciales.
}

export default Icredentials;